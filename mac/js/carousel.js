// glowplay recommendation

document.addEventListener('DOMContentLoaded', ()=> {
    const slides = document.querySelector('.recomend-slide');
    const prevButton = document.querySelector('.recommend-button.prev');
    const nextButton = document.querySelector('recommend-button.next');
    const indicators = document.querySelector('indicator')
    const carouselContainer = document.querySelector('.recommend-container')
    let currenIndex = 0; 

    function showSlide(index) {
        const slideWidth = slide[0].clientWidth;
        
    }
})